package Behaviours;

//This is an interface which has the method takeDamage.
//Any class that implements this interface has to use this method.
//When implemented this method has to take in an int.

public interface iDamagable {

    void takeDamage(int attackPoints);
}
